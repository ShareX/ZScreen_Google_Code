namespace ExtensionManager
{
    public enum SourceFileLanguage
    {
        Unknown,
        CSharp,
        Vb,
        Javascript
    }

    public enum ExtensionType
    {
        Unknown,
        Compiled,
        SourceFile
    }
}